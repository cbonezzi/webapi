README.TXT

This is a webapi application build on .NET Framework 4.7.2. It has an SQL Server backend to store data.

There is a SQL Project to publish the database to SQL, called WA.SQL. It can be published directly from Visual studio using publish mechanism.

CONFIGS:
--------
connections.config file under WA1 WebApi Project needs to be modified to target you SQL Server.
